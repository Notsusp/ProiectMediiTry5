@page
@model ProiectMediiTry5.Pages.Jobs.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job[0].Titlu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job[0].Descriere)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job[0].TermenLimita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job[0].Finalizat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job[0].Project.ProjectName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Job) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Titlu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descriere)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TermenLimita)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Finalizat)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Project.ProjectName)
                </td>

            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
